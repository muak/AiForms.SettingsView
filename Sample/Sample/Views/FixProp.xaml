<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:sv="clr-namespace:AiForms.Renderers;assembly=SettingsView"
    xmlns:l="clr-namespace:AiForms.Layouts;assembly=AiForms.Layouts"
    xmlns:ef="clr-namespace:AiForms.Effects;assembly=AiForms.Effects"
    x:Class="Sample.Views.FixProp">

    <sv:SettingsView x:Name="settings">
        <sv:Section IsVisible="{Binding IsShowPurchased.Value}" ItemsSource="{Binding PurchasedProducts}" >
            <sv:Section.ItemTemplate>
                <DataTemplate>
                    <sv:LabelCell Title="{Binding Name}" ValueText="{Binding Price}" Description="{Binding ExpiredDate}"
                                   />
                </DataTemplate>
            </sv:Section.ItemTemplate>
        </sv:Section>
        
        <sv:Section ItemsSource="{Binding SaleProducts}" >
            <sv:Section.ItemTemplate>
                <DataTemplate>
                    <sv:CommandCell Title="{Binding Name}" ValueText="{Binding Price}" IsEnabled="{Binding BindingContext.IsAvailable.Value,Source={x:Reference settings}}"
                                    Command="{Binding BindingContext.PurchaseCommand,Source={x:Reference settings}}" CommandParameter="{Binding Id}"  />
                </DataTemplate>
            </sv:Section.ItemTemplate>
        </sv:Section>

    </sv:SettingsView>
</ContentPage>
